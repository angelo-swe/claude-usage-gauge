# Credentials — never commit
.env
*.env

# Xcode build artifacts
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
xcuserdata/
*.xccheckout
*.moved-aside

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but just in case)
Pods/

# Local project notes — never publish
CLAUDE.md
